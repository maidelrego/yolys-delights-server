{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "order",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "email": {
      "type": "string"
    },
    "stripeId": {
      "type": "text"
    },
    "products": {
      "type": "json"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "success",
        "cancel"
      ],
      "default": "cancel"
    },
    "customerName": {
      "type": "string"
    },
    "orderTypeDate": {
      "type": "datetime"
    },
    "orderType": {
      "type": "enumeration",
      "enum": [
        "delivery",
        "pickup"
      ]
    },
    "address": {
      "type": "string"
    },
    "notes": {
      "type": "string"
    }
  }
}
